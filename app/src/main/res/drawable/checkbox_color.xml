<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Quand coché -->
    <item android:color="@color/success" android:state_checked="true"/>
    <!-- Quand non coché -->
    <item android:color="@color/gray"/>
</selector>
